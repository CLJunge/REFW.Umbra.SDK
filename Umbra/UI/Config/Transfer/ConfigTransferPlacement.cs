namespace Umbra.UI.Config.Transfer;

/// <summary>
/// Defines where the built-in config transfer UI is rendered relative to the main config UI.
/// </summary>
public enum ConfigTransferPlacement
{
    /// <summary>
    /// Renders the transfer UI before the main config UI.
    /// </summary>
    BeforeConfig,

    /// <summary>
    /// Renders the transfer UI after the main config UI.
    /// </summary>
    AfterConfig
}
